---
---
<header class="bg-white shadow-md fixed w-full top-0 z-50">
    <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <a href="#" class="text-2xl font-bold text-blue-900 flex items-center gap-2">
            <span>🏠</span>
            <span>Karen </span>
        </a>

        <nav class="hidden md:flex space-x-8 items-center">
            <a href="#inicio" class="text-gray-600 hover:text-blue-600 font-medium transition">Inicio</a>
            <a href="#beneficios" class="text-gray-600 hover:text-blue-600 font-medium transition">Beneficios</a>
            <a href="#ubicacion" class="text-gray-600 hover:text-blue-600 font-medium transition">Ubicación</a>
        </nav>

        <a href="#agenda" class="hidden md:inline-block bg-blue-600 text-white px-5 py-2 rounded-full font-bold hover:bg-blue-700 transition shadow-lg">
            Agendar Cita 📅
        </a>

        <button id="menu-btn" class="md:hidden text-gray-700 focus:outline-none">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
        </button>
    </div>

    <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-100 p-4 absolute w-full">
        <div class="flex flex-col space-y-4">
            <a href="#inicio" class="text-gray-700 hover:text-blue-600">Inicio</a>
            <a href="#beneficios" class="text-gray-700 hover:text-blue-600">Beneficios</a>
            <a href="#ubicacion" class="text-gray-700 hover:text-blue-600">Ubicación</a>
            <a href="#agenda" class="text-blue-600 font-bold">¡Agendar Cita Ahora!</a>
        </div>
    </div>
</header>

<script>
    // Pequeño script para que funcione el botón en celulares
    const menuBtn = document.getElementById('menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');

    if (menuBtn && mobileMenu) {
        menuBtn.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });
    }
</script>